# Ads Center Sync

The Admin Center advertising section is grouped under `ads` and contains:

- `AdManager` — مدير الإعلانات
- `AdvertiseWithUs` — أعلن معنا
- `AdvertiserPortal` — بوابة المعلن

This marker is only used to trigger the normal repository deployment pipeline. The three page components remain independent and are not modified.
